<!DOCTYPE html>
<html>
<head>
	<title>New Cat!</title>
	<style type="text/css">
		* {
			margin: 0px;
			padding: 0px;
			font-family: helvetica;
		}
		body {
			padding: 20px;
		}
		form {
			padding: 20px;
			width: 300px;
		}
		label, input {
			margin: 10px 0px;
		}
		textarea {
			width: 100%;
			height: 70px;
		}
		p {
			margin: 10px 0px;
			color: red;
		}
	</style>
</head>
<body>
	<h2>Become a Cat!</h2>
	<hr>
	<% if(typeof(errors)!=='undefined') {%>
		<% for (var x in errors) {%>
			<p><%= errors[x]%></p>
		<%}%>
	<%}%>
	<form action="/cat" method='POST'>
		<label for='name'>Name: </label>
		<input type="text" name="name"><br>
		<label for='description'>Description: </label>
		<textarea name="description"></textarea>
		<input type="submit" name="submit" value="Meow">
	</form>
</body>
</html>